import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inner Circle | VSC Capital & Advisory",
  description: "Execute high-probability setups, share observation logs, and join the premium momentum execution room.",
};

export default function InnerCircleLayout({ children }: { children: React.ReactNode }) {
  return children;
}
