import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Offerings | VSC Capital & Advisory",
  description: "Explore VSC Capital offerings including the Learning Hub, VSC Advantage portfolio advisory, and the Inner Circle execution room.",
};

export default function OfferingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
