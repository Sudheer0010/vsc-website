import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Strategic Discussion | VSC Capital & Advisory",
  description: "Begin a strategic discussion with VSC to map your capital goals, risk boundaries, and trading processes.",
};

export default function EnquireLayout({ children }: { children: React.ReactNode }) {
  return children;
}
