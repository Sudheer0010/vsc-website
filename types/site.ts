export interface SiteConfig {
  name: string;
  title: string;
  description: string;
  url: string;
  stats: {
    notesLabel: string;
    lettersLabel: string;
    frameworksLabel: string;
    readingsLabel: string;
  };
}
